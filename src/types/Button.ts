
interface ButtonTypes {
    title: string;
    onClick?: () => void; // Assuming onClick is a function that doesn't take any arguments and doesn't return anything
  }
  
  export default ButtonTypes;