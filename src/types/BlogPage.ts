export interface BlogType{
    id: number,
    image: String,
    published_date: String,
    title: String,
    slug: String
}

export interface BlogDetails{
    id: number,
    image: String,
    postedTime: String,
    title: String,
    details: string
}